---
name: retouch-automation
description: いけばな・生花作品の展示写真（Sony ARW）を一括処理し、作品検出・傾き補正・トリミング・レタッチの設定を Lightroom 用の XMP サイドカーとして書き出す。写真の採用可否は判断せず全枚数を処理し、同じ作品の写真には共通の分類タグを付けて Lightroom 側で重複を外せるようにする。「いけばなの写真を処理して」「作品写真の XMP を作って」「展示写真をまとめてトリミングして」「RAW を一括で傾き補正して」「この撮影分を Lightroom 用に整えて」で発火。
---

# retouch-automation

生花作品の RAW 写真を一括処理し、各 RAW と同名の XMP サイドカーを書き出す。

**RAW ファイルは変更・移動・削除しない。** 写真の採用・不採用も判断しない。
最後は人が Lightroom で分類タグを使って重複を外す、という運用を前提にしている。

対象は「白い壁や幕を背にした個別の作品」を撮った展示写真。
暗い会場でのステージ記録のような、1 枚に作品が写っていない写真は対象外。

## 実行

```sh
uv sync --python 3.12 --extra models
uv run retouch-automation process /path/to/photos
```

**`--extra models` は事実上必須。** 省くとモデルを使わないバックエンドに落ちるが、
その状態では分類がほぼ機能しない（後述）。

初回はモデルを Hugging Face から取得する（合計 1.5GB 程度、`~/.cache/huggingface`）。
Apple Silicon では MPS を使い、使えなければ自動で CPU に落ちる。

| オプション | 内容 |
| --- | --- |
| `--config <json>` | しきい値や余白量を上書きする |
| `--overwrite-xmp` | 既存 XMP を丸ごと置き換える |

出力：

- `<raw_dir>/*.xmp` … RAW と同名のサイドカー
- `<raw_dir>/.retouch-automation/` … プレビュー・マスク・確認用画像・レポート

## 運用の順序（重要）

**XMP を作ってから Lightroom に読み込む。** この順を守れば何も問題ない。

逆順にすると手間が増える。Lightroom は動作中にサイドカーを読み直さないので、
取り込み済みの写真に後から XMP を置いても反映されず、**Lightroom の再起動が要る**。

既存の XMP がある場合、既定では**触らない**（`on_existing: skip`）。
人が Lightroom で直した回転・トリミングを再実行で消さないため。
ツールを改良して全部作り直したいときだけ `--overwrite-xmp` を付ける。

## 結果の確認

```sh
cat <raw_dir>/.retouch-automation/report.json
```

`needs_review` に要確認の写真が並ぶ。`failed` が空なら全枚数に XMP が出ている。

目で見るなら各写真の `overlay/` を開く。

- `detection.jpg` … 検出した作品領域（緑のマスクと赤い枠）
- `straighten.jpg` … 水平推定に使った基準線
- `crop.jpg` … 最終的なトリミング範囲

検出枠が背景全体に広がっていたり、画面端に達している写真は、
T5 が全画面フォールバックに倒している。切り落としてはいないので害は無い。

## うまくいかないときに見るところ

**分類が 1 グループにまとまってしまう**
撮影日時が取れていない可能性がある。`result.json` の `raw.captured_at` を見て、
全ファイルが同じ時刻ならファイル更新時刻にフォールバックしている
（コピーや書き出しで全部同時刻になる）。

**分類が細かく割れすぎる / 別の作品が混ざる**
`classify.backend` が `classical` に落ちていないか `result.json` の
`classification.detail.backend` で確認する。色ヒストグラムでは
同一作品と別作品の類似度が 0.2% しか離れず、実質的に判別できない。
撮影間隔も作品の切れ目が 6 秒しか空かないことがあり当てにならない。
**DINOv2 を使って初めて分類が成立する。**

**検出が画像全体になる**
`detection.detail.backend` を見る。`model` なのに全画面なら、位置推定の確信度が
低くてマスクが退化し classical に譲った状態。`classical` なら背景幕を拾っている
可能性が高い（照明が暗く幕がベージュ系のときに起きやすい）。

**処理が遅い**
モデルありで 1 枚あたり数秒。13 枚で 40 秒程度が目安。
`models.device` に `cpu` が入っていないか確認する。

## 構成

| ツール | 役割 |
| --- | --- |
| T1 | RAW 読み込み。プレビュー生成、EXIF から撮影日時・ISO・絞り |
| T2 | 写真分類。DINOv2 の特徴量＋撮影間隔で同じ作品をまとめる |
| T3 | 作品検出。GroundingDINO で位置、SAM 2.1 で領域 |
| T4 | 水平推定。背景の直線から傾きを求める |
| T5 | トリミング。マスク優先、端寄りは全画面へ倒す |
| T6 | レタッチ。白飛び・黒つぶれの張り付きから Highlights / Shadows のみ |
| T7 | XMP 生成 |
| T8 | オーケストレーター |

しきい値や余白量はすべて `config.py` に出してある。コードに数値を埋めない。

## 詳しい資料

- [要求仕様](./docs/SPEC.md)
- [設計と、実機で確定させたこと](./docs/DESIGN.md)
- [XMP の書式（Lightroom の実物と照合した結果）](./docs/xmp.md)

座標変換は `geometry.py` に集約してある。**事故が起きるのはほぼここなので、
変換をこのモジュールの外に書かない。** `crs:Crop*` は軸平行の外接矩形ではなく
傾いた枠の対角 2 頂点、という直感に反する定義になっている。docs/xmp.md を読むこと。
